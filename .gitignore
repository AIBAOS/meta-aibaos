# Kas lockfile (generate with kas lock)
kas/lock.yml

# Local configuration (user-specific)
kas/local.yml

# Documentation intermediates
*.json

# Build artifacts
build/
tmp/
sstate-cache/
downloads/
